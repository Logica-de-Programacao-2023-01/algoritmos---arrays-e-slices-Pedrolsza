package main

import "fmt"

func main() {
	array := []int{1, 2, 3, 4, 6, 7, 8, 9, 0}
	var soma int
	for i := 0; i < len(array); i += 2 {
		soma += array[i]
	}
	fmt.Println("A soma dos numeros nas posições pares é:", soma)
}
